# Updated YieldVault Contract

This is an updated version of the minimal YieldVault contract with hard-coded initialization values.

## Changes

- Modified the initialize function to use hard-coded values for name, symbol, asset_name, asset_symbol, and decimal_offset
- Removed parameters from the Initialize message
- Added proper storage for token metadata and accounting state
