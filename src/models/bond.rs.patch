// This is a patch to temporarily modify the is_mature function for testing
// We'll add an override for the --modify-test flag

use slop::utils::BlockContext;

pub trait BondMaturityCheck {
    fn is_mature(&self, context: &dyn BlockContext) -> bool;
    fn override_maturity_check(&self) -> bool;
}

// Then we'll implement this in the orbital_bond_collection.rs file
// to force bonds to be considered mature for testing purposes
