// This is a patch file for implementing the update_bonds_for_test method
// to be added to the OrbitalBondCollection struct

// Add this method to the impl block for OrbitalBondCollection:

/// Test-only method to update bonds using a provided callback function
/// This should ONLY be used for testing purposes
#[cfg(any(test, feature = "testing"))]
pub fn update_bonds_for_test<F>(&mut self, update_fn: F)
where
    F: Fn(&mut Bond),
{
    // Apply the provided callback to each bond in the collection
    for bond in self.bonds.iter_mut() {
        update_fn(bond);
    }
}
